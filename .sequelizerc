const { resolve } = require("path");

module.exports = {
  config: resolce(__dirname, "src", "config", "database.js"),
  "models-path": resolce(__dirname, "src", "app", "models"),
  "migrations-path": resolce(__dirname, "src", "database", "migrations"),
  "seeders-path": resolce(__dirname, "src", "database", "seeds")
};
